federation_version: =2.3.2
subgraphs:
  # Comment out the following sections if you are not using the respective service
  influx:
    schema:
      subgraph_url: http://influxdb-proxy:8082/query
  opcua:
    schema:
      subgraph_url: http://opcua-proxy:8083/query
  rhize:
    routing_url: http://rhize:8080/graphql
    schema:
      subgraph_url: http://localhost:8080/graphql # fetch schema via libre-baas-proxy
  ## Add other supergraphs here as required e.g.
  # spacex:
  #  routing_url: https://spacex-production.up.railway.app
  #  schema:
  #    subgraph_url: https://spacex-production.up.railway.app
